import React from 'react'

const Alumni = ({ location }) => {
  // This is for the fetching the user data
  console.log(location.state.id)
  return (
    <div>
      <h1>Some stuff in here, this is the alumni page </h1>
    </div>
  )
}

export default Alumni
